# Never commit credentials or the live inventory
id_rsa
id_rsa.pub
*.pem
*.key
# For learning purposes inventory is included in the repo, but it should not be committed in a real project
# inventory

# Collections installed at sync time
collections/ansible_collections/

# Junk
.DS_Store
*.tgz
*.zip

# IBM VSCode extension automatically adds this 
zapp.yaml
