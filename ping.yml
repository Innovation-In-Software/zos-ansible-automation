---
- name: Ping z/OS mainframe
  hosts: mainframe
  collections:
    - ibm.ibm_zos_core
  gather_facts: false
  environment: "{{environment_vars}}"
  
  tasks:
    - name: Pinging host
      ping: {}
      register: result
      
    - name: Response
      debug:
        msg: "{{ result.ping }}"
